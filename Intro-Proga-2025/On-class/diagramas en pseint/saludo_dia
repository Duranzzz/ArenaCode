Algoritmo sin_titulo
	definir hora como entero
	escribir "Ingrese la hora"
	leer hora
	
	si (hora >= 6 y hora <= 10 ) Entonces
		Escribir "Buenos dias"
	FinSi
	
	si (hora >= 11 y hora <= 17 ) Entonces
		Escribir "Buenas tardes"
	FinSi
	
	si (hora >= 18  y hora <= 22 ) Entonces
		Escribir "Buenas noches"
	FinSi
	si (hora >= 1 y hora <= 5 ) Entonces
		Escribir "Buenas madrugadas"
	SiNo
		si (hora >= 23 y hora <= 24)
			Escribir "Buenas madrugadas"
		FinSi
	FinSi
	
	si (hora < 0) entonces
		escribir "No se aceptan numeros negativos"
	SiNo
		si (hora > 24) entonces
			Escribir "Numero superior a 24, INVALIDO"
		FinSi
	FinSi
FinAlgoritmo
